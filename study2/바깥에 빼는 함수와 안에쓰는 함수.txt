function getNumbers() {
    const candidate = [1,2,3,4,5,6,7,8,9]
    const array = [];
    for (let i=0; i<4; i+= 1) {
        const chosen = candidate.splice(Math.floor(Math.random() * (9-i)), 1)[0];
        array.push(chosen)
    }
    return array;
}

class NumberBaseball extends Component {


 getNumbers() 같은 함수는 this 를 쓸 일 없으면, 뺌.